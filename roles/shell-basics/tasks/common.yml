- name: Install oh-my-zsh
  git:
    repo: https://github.com/robbyrussell/oh-my-zsh.git
    dest: ~/.oh-my-zsh
  become_user: "{{ user }}"

- name: Change shell to zsh
  user:
    name: "{{ user }}"
    shell: /bin/zsh
